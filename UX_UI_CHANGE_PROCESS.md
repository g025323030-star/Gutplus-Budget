# תהליך שינוי UX/UI מומלץ לגוטפלוס

## מטרת המסמך

המסמך הזה מגדיר תהליך שינוי מומלץ למערכת גוטפלוס לפני התחלת עבודה על עיצוב או קוד. המטרה היא לא "לשפר צבעים" נקודתית, אלא להבין את המערכת כפי שהיא עובדת בפועל, לזהות איפה המשתמשים מאבדים בהירות, ואז לבצע שינוי מדורג שמחזק אמון, מקצועיות ויכולת עבודה יומיומית.

הכיוון המומלץ כרגע הוא מערכת פיננסית בסגנון **פרימיום יועצים**: שקטה, ברורה, מדויקת, אמינה, ופחות דומה לאפליקציית צרכן צבעונית.

## עיקרון מוביל

לא מחליטים על redesign לפני שמריצים את המערכת ורואים אותה חיה.

קריאת קוד מסבירה איך המערכת בנויה, אבל UX אמיתי מתגלה רק כשעוברים במסכים, מנסים לבצע פעולות, רואים עומסים, מצבי ריק, שגיאות, מובייל, RTL וקצב עבודה. לכן כל שינוי עיצובי חייב להתחיל מביקורת חיה של המוצר.

## שלב 1: הרצה וביקורת חיה

### מה עושים

מריצים את המערכת מקומית, נכנסים למסכים המרכזיים ובודקים אותם בדסקטופ, טאבלט ומובייל.

פקודות בסיסיות להרצה:

```bash
cd /Users/nechama/Gutplus-Budget
cd server && docker-compose up -d
cd ..
npm run build:shared
npm run dev:server
```

בטרמינל נוסף:

```bash
cd /Users/nechama/Gutplus-Budget
npm run dev:client
```

פותחים:

```text
http://localhost:5173
```

### מסכים לבדיקה

- `/login` — רושם ראשוני, אמון, בהירות פעולה, טיפול בשגיאות.
- `/welcome` — onboarding, סדר הפעולות, תחושת ליווי.
- `/snapshot` — תמונת מצב, היררכיית נתונים, הבנה מה חשוב עכשיו.
- `/expenses` — הזנת הוצאות, קטגוריות, טבלאות, עריכה, שמירה, מובייל.
- `/income` — עקביות מול הוצאות, קלות הזנה, דפוסים חוזרים.
- `/budget` — תחזית, השוואה, טבלה שנתית, עומס מידע.

### למה זה חשוב

אם מדלגים על השלב הזה, יש סיכון לשנות את השפה הוויזואלית בלי לפתור את בעיית הבהירות. במערכת פיננסית, משתמש לא צריך רק "יפה"; הוא צריך להבין מה מצבו, מה נדרש ממנו, ומה קרה אחרי כל פעולה.

## שלב 2: מיפוי בעיות לפי מסע משתמש

### מה עושים

מתעדים לכל מסך:

- מה המשתמש בא לעשות כאן.
- מה הפעולה המרכזית במסך.
- מה לא ברור במבט ראשון.
- איפה יש עומס ויזואלי או קוגניטיבי.
- איפה חסר feedback אחרי פעולה.
- איפה מובייל או RTL שוברים את החוויה.
- אילו רכיבים חוזרים מופיעים בכמה מסכים אבל נראים או מתנהגים אחרת.

### למה זה חשוב

שינוי מוצלח צריך להתחיל מהתנהגות המשתמש, לא מקומפוננטות. רק אחרי שמבינים את המסע, אפשר להחליט אם הבעיה היא צבע, מבנה, wording, ניווט, טבלה, או state שלא מסביר את עצמו.

## שלב 3: קביעת עקרונות עיצוב מחייבים

### מה עושים

לפני שינוי קוד, מנסחים 5-7 עקרונות שינחו את כל המסכים. לדוגמה:

- כל מסך חייב לענות במבט ראשון: איפה אני, מה המצב, ומה הפעולה הבאה.
- מידע פיננסי קריטי מקבל היררכיה גבוהה; מידע תומך נדחה לשכבה שנייה.
- צבעי סטטוס משמשים למשמעות, לא לקישוט.
- מובייל הוא חוויית עבודה אמיתית, לא רק גרסה נדחסת של דסקטופ.
- פעולות שמירה, מחיקה ושגיאה חייבות feedback ברור.
- אין שני רכיבים שונים לאותה פעולה אם אין סיבה מוצרית חזקה.

### למה זה חשוב

בלי עקרונות, כל מסך עלול להשתפר לבד אבל המוצר יישאר לא אחיד. עקרונות הופכים את העבודה מהחלפת סגנון לבניית מערכת מוצרית.

## שלב 4: בניית foundation לפני שיפוץ מסכים

### מה עושים

לפני שנוגעים במסכים גדולים, מגדירים שכבת בסיס:

- צבעים וטוקנים ב־`client/tailwind.config.js`.
- טיפוגרפיה וריווחים ב־`client/src/styles/index.css`.
- קומפוננטות UI משותפות תחת `client/src/components/ui/`.
- דפוסי `Button`, `Card`, `Input`, `FormField`, `Modal`, `SegmentedControl`, `EmptyState`, `Skeleton`, `PageHeader`.
- כללי motion קבועים כדי שלא יהיו אנימציות שונות בכל מסך.

### למה זה חשוב

אם מתחילים ישר ממסך כמו הוצאות או דשבורד, נוצרת שכבה נוספת של פתרונות חד־פעמיים. foundation טוב מאפשר לשפר מהר יותר, לשמור על אחידות, ולהקטין סיכון לרגרסיות ויזואליות.

## שלב 5: שיפור מעטפת וניווט

### מה עושים

משפרים את `Layout` ואת `Sidebar`:

- מיקום ברור במסך.
- active state שלא מסתמך רק על צבע.
- ניווט נוח במובייל.
- פחות תלות ב־hover כדי להבין איפה נמצאים.
- header/context שמסביר את המסך הנוכחי.

### למה זה חשוב

ניווט הוא שכבת ההתמצאות של המערכת. אם המשתמש לא מבין איפה הוא נמצא, גם מסכים יפים ירגישו לא בטוחים. במוצר פיננסי, תחושת שליטה חשובה כמעט כמו הנתונים עצמם.

## שלב 6: טיפול במסך הוצאות והזנת נתונים

### מה עושים

המסך הזה צריך לקבל עדיפות גבוהה כי הוא מסך עבודה, לא רק תצוגה.

בודקים ומשפרים:

- יצירת שורה חדשה.
- בחירת קטגוריה.
- מצב עריכה מול מצב צפייה.
- שמירה אוטומטית או מפורשת.
- שגיאות validation.
- סטטוס שמירה.
- מודלים מתקדמים כמו תשלומים, דו־חודשי וחזרתיות.
- התנהגות מובייל.

### למה זה חשוב

אם הזנת הנתונים לא ברורה, כל הדשבורדים והתחזיות מאבדים אמינות. המסך הזה הוא מקור האמת של המערכת ולכן עליו להיות הכי ברור, הכי סלחני והכי יציב.

## שלב 7: שיפור Snapshot ודשבורדים

### מה עושים

מסדרים מחדש את היררכיית המידע:

- תובנה מרכזית אחת בראש המסך.
- שלושה KPI עיקריים בלבד בשכבה הראשונה.
- פירוט וגרפים בשכבה שנייה.
- רשימות או breakdowns בשכבה שלישית.
- בחינה מחדש של pie chart אם הוא עמוס מדי בעברית/RTL.

### למה זה חשוב

דשבורד טוב לא מציג את כל הנתונים; הוא אומר למשתמש מה להבין עכשיו. במוצר ייעוצי־פיננסי, היעד הוא להרגיש כמו תמונת מצב מקצועית, לא כמו אוסף כרטיסים.

## שלב 8: שיפור Budget Planner

### מה עושים

מחזקים את דף התקציב ככלי תחזית:

- הבהרה בין תחזית חודשית לטבלה שנתית.
- הצגת חריגות וסיכונים בצורה מובנת.
- שיפור קריאות הטבלה השנתית.
- שימוש באותם KPI/cards כמו בשאר המערכת.
- בדיקת sticky columns וגלילה ב־RTL.

### למה זה חשוב

תקציב הוא לא רק טבלה; הוא כלי החלטה. אם המשתמש לא מבין איפה יש חריגה, מה צפוי לקרות, ומה צריך פעולה, המסך לא ממלא את התפקיד שלו.

## שלב 9: Login ו־Onboarding

### מה עושים

משפרים את הרושם הראשוני:

- שפה ויזואלית רגועה ויוקרתית יותר.
- טופס נקי וברור.
- טיפול טוב בשגיאות והמתנה.
- onboarding שמרגיש כמו ליווי, לא כמו מילוי טופס.

### למה זה חשוב

זה המקום שבו המשתמש מחליט אם המערכת מרגישה אמינה. במיוחד במוצר פיננסי, first impression חייב לשדר מקצועיות, פרטיות וסדר.

## שלב 10: נגישות, RTL ו־QA ויזואלי

### מה עושים

בודקים לפני סיום:

- tab order.
- focus states.
- aria labels לאייקונים.
- Escape במודלים.
- contrast לצבעי סטטוס וזהב.
- טקסטים בעברית שלא נחתכים.
- מובייל ברוחב 390px.
- טאבלט סביב 768px.
- דסקטופ 1024px ו־1440px.

### למה זה חשוב

נגישות ו־RTL הם לא polish בסוף; הם חלק מהשימושיות הבסיסית של המוצר. במערכת עברית פיננסית, טקסט חתוך, כיוון לא טבעי או focus לא ברור פוגעים ישירות באמון.

## סדר עדיפויות מומלץ

1. להריץ ולבצע audit חי.
2. להגדיר עקרונות עיצוב ומפת בעיות.
3. לבנות foundation משותף.
4. לשפר ניווט ומעטפת.
5. לשפר הוצאות והזנת נתונים.
6. לשפר Snapshot.
7. לשפר Budget Planner.
8. לשפר Login ו־Onboarding.
9. לבצע QA נגישות, RTL ומובייל.

## חלוקה מומלצת לאג׳יינטים או משימות

### Agent 1: Audit חי ותיעוד

מריץ את המערכת, עובר במסכים, מתעד בעיות לפי מסע משתמש, ומחזיר רשימת ממצאים עם screenshots או תיאורים מדויקים.

### Agent 2: Design foundation

מגדיר טוקנים, טיפוגרפיה, קומפוננטות UI בסיסיות וכללי motion.

### Agent 3: Navigation and shell

מטפל ב־Layout, Sidebar, מובייל, active states והתמצאות.

### Agent 4: Expenses data-entry

מטפל בהוצאות, טבלאות, קטגוריות, עריכה, שמירה, שגיאות ומודלים מתקדמים.

### Agent 5: Dashboards and planning

מטפל ב־Snapshot, Budget Planner, KPI, גרפים, טבלאות שנתיות ו־empty/loading/error states.

### Agent 6: Auth and onboarding

מטפל ב־Login, reset password, Welcome flow ורושם ראשוני.

## מדדי הצלחה

- משתמש מבין בכל מסך מה המצב ומה הפעולה הבאה תוך כמה שניות.
- הזנת הוצאה חדשה לא דורשת ניחוש או ניסוי וטעייה.
- שמירה, שגיאה ומחיקה מקבלות feedback ברור.
- המערכת נראית כמו מוצר פיננסי מקצועי ואחיד, לא אוסף מסכים.
- מובייל ו־RTL עובדים כחוויה ראשית.
- רכיבים חוזרים נראים ומתנהגים באופן עקבי.

## מה לא לעשות כרגע

- לא להתחיל להחליף צבעים לפני audit חי.
- לא לשפץ מסך אחד בלי foundation משותף.
- לא לשנות backend או מודל נתונים כחלק משינוי ויזואלי, אלא אם audit מוכיח שזה הכרחי.
- לא להוסיף אנימציות כדי להסתיר חוסר בהירות.
- לא לפתור בעיית UX באמצעות טקסט הסבר ארוך בתוך המסך.

## החלטת עבודה מומלצת

הצעד הבא הנכון הוא להריץ את המערכת, לבצע audit חי קצר ומדויק, ואז לבחור יחד את שלב 1 ליישום. רק אחרי שיש ממצאים מהמערכת החיה כדאי להתחיל לערוך קוד.