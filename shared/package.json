{
  "name": "@gutplus/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared contracts, types, schemas, and constants between client and server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
